os: linux
dist: trusty
# rvm: 2.0.0
language: ruby
bundler_args: "--without development --deployment"
sudo: false
before_install:
- gem install bundler -v '< 2.0'
- rvm list known
- rvm install 2.0.0-p648
stage: test
script:
- echo "Running ruby"
- ruby -v
