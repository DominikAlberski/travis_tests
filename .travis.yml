os: linux
dist: xenial
language: ruby
bundler_args: "--without development --deployment"
sudo: false
before_install:
- rvm install 2.0.0-p648
- gem install bundler -v '< 2.0'
- rvm use 2.0.0-p648
stage: test
script:
- echo "Running ruby"
- ruby -v
