os: linux
rvm: 2.0.0
language: ruby
dist: trusty
bundler_args: "--without development --deployment"
sudo: false
before_install:
- gem install bundler -v '< 2.0'
- rvm list known
# - rvm install $(< .ruby-version)
stage: test
script:
- echo "Running ruby"
- ruby -v
